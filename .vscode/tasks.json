{
    "tasks": [
        {
            "type": "shell",
            "label": "Build proyecto con make",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Compila el proyecto completo usando el Makefile."
        }
    ],
    "version": "2.0.0"
}